/*
import {BaseBusiness} from '../../BaseBusiness'
import { ITransactionTransit } from './transit.types';
import { TransactionTransitRepository } from './transit.repository';


class TransactionTransitBusiness extends BaseBusiness<ITransactionTransit> {
    private _transactionTransitRepository: TransactionTransitRepository;

    constructor() {
        super(new TransactionTransitRepository())
        this._transactionTransitRepository = new TransactionTransitRepository();
    }
}

Object.seal(TransactionTransitBusiness);
export = TransactionTransitBusiness;*/
