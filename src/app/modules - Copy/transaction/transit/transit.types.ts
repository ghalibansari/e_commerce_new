/*
import { ICompany } from "app/modules/company/company.types";
import {Document} from "mongoose";
import { ISku } from "../../sku/sku.types";
import { IUser } from "../../user/user.types";


export interface ITransactionTransit extends Document {
    _id: string;
    skuIds: ISku['_id'][];
    transactionId: string;
    companyId: ICompany['_id'];
    isActive: boolean;
    isDeleted: boolean;
    createdBy:IUser['_id'];
    updatedBy:IUser['_id'];
    createdAt:Date;
    updatedAt:Date;
}*/
